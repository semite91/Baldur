# Windows-local only

Baldur Recognition runs only on the user's Windows machine with local inference; no cloud, no cross-platform support. We chose this because posture video must never leave the device and the WPF host is Windows-only, trading reach and scaling for privacy and simpler deployment.
